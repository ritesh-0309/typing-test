import type { Metadata, Viewport } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const viewport: Viewport = {
  width: "device-width",
  initialScale: 1,
};

export const metadata: Metadata = {
  title: "TypeRush — Minimalist Speed Typing Master on Catalyst Slate",
  description: "An ultra-smooth, responsive typing speed test and trainer built with Next.js and deployed on Zoho Catalyst Slate frontend hosting.",
  keywords: ["typing test", "speed typing", "monkeytype clone", "catalyst slate", "zoho catalyst", "nextjs"],
  authors: [{ name: "TypeRush" }],
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html
      lang="en"
      className={`${geistSans.variable} ${geistMono.variable} h-full antialiased`}
    >
      <body className="min-h-full flex flex-col">{children}</body>
    </html>
  );
}
